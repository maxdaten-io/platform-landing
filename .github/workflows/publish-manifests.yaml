name: Publish Manifests

on:
  push:
    branches: [main]
    paths: ["k8s/**"]
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  publish:
    uses: maxdaten-io/platform-devenv/.github/workflows/publish-manifests.yaml@main
    secrets: inherit
